MOVES["Ingrain"] = {
    description = "The user lays roots that restore its HP on every turn.",
    category = MOVE_CATEGORY.STATUS,
    clientIconId = 26930,
    iconId = 0,
    dType = DAMAGE_TYPE_GRASS,
    type = SKILLS_TYPES.STATS,
    -- requiredEnergy = 100,
    requiredEnergy = 0,
    damage = 0,
    damageType = ELEMENT_GRASS,
    effect = EFFECT_ROOTS_FOUR,
    projectile = nil,
    maxDistance = nil,
    cooldownTime = 120,
    cooldownStorage = 15367,
    makeHeal = true,
    wildBlock = true,
}