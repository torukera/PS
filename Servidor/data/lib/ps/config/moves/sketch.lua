MOVES["Sketch"] = {
    description = "Sketch allows the user to copy the opponent's last move permanently, therefore replacing Sketch.",
    mimicable = false,
    sketchable = false,
    category = MOVE_CATEGORY.STATUS,
    clientIconId = 16325,
    iconId = 0,
    dType = DAMAGE_TYPE_NORMAL,
    functionName = "Sketch",
    type = SKILLS_TYPES.TARGET,
    -- requiredEnergy = 100
    requiredEnergy = 0,
    requiredLevel = 15,
    damage = 0,
    damageType = ELEMENT_NORMAL,
    effect = EFFECT_SKETCH,
    projectile = nil,
    maxDistance = 1,
    cooldownTime = 3,
    cooldownStorage = 15286
}