MOVES["Memento"] = {
    description = "Memento causes the user to faint and in return lowers the Attack and Special Attack stat of the target by two stages each.",
    category = MOVE_CATEGORY.STATUS,
    clientIconId = 27581,
    iconId = 0,
    dType = DAMAGE_TYPE_DARK,
    type = SKILLS_TYPES.TARGET,
    -- requiredEnergy = 50,
    requiredEnergy = 0,
    damage = 0,
    damageType = ELEMENT_DARK,
    effect = EFFECT_BIG_TEAR_DROP,
    projectile = PROJECTILE_GRAVEL,
    maxDistance = 6,
    cooldownTime = 60,
    cooldownStorage = 15433,
    makeContact = false
}