MOVES["Destiny Bond"] = {
    description = "After using this move, if the user faints, the Pokemon that landed the knockout hit also faints. Its chance of failing rises if it is used in succession.",
    category = MOVE_CATEGORY.STATUS,
    clientIconId = 26907,
    iconId = 0,
    dType = DAMAGE_TYPE_GHOST,
    type = SKILLS_TYPES.STATS,
    -- requiredEnergy = 120,
    requiredEnergy = 0,
    damage = 0,
    damageType = ELEMENT_GHOST,
    effect = EFFECT_DARK_SHADOW_DOWN,
    projectile = nil,
    cooldownTime = 60,
    cooldownStorage = 15371
}